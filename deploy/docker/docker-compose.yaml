version: "3.9"

services:
  lock-manager:
    build:
      context: "../../"
      dockerfile: "./Dockerfile"
    depends_on: ["storage"]
    restart: "always"
    expose: [8082]
    ports: ["8082:8082"]

  storage:
    expose:
      - 9090
